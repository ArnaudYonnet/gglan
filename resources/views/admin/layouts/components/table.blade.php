<table id="{{ $id }}" class="table table-bordered table-hover {{ $class }}">
    <thead>
        <tr>
            {{ $headers }}
        </tr>
    </thead>
    <tbody>
        {{ $content }}
    </tbody>
</table>